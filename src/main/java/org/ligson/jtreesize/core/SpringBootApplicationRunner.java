package org.ligson.jtreesize.core;

public class SpringBootApplicationRunner {
    public static void run(Class<?> mainClass, String[] args) throws Exception {
        ApplicationContext context = new ApplicationContext(mainClass);
        // Additional logic to start the application can be added here
    }
}
